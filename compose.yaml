services:
  validator:
    image: amsterdam/rfc9068-access-token-validator
    build:
      context: .
      dockerfile: Containerfile
    volumes:
      - ".:/app:rw,Z"
    develop:
      watch:
        - action: rebuild
          path: ./uv.lock
